import 'jest'; // Importa el framework de pruebas Jest

// Describe un grupo de pruebas con un nombre, en este caso 'Example'
describe('Example', () => {
  // Define una prueba con un mensaje descriptivo
  it('should pass', () => {
    expect(true).toBe(true); // Se espera que el valor true sea igual a true
  });
});
